## Install server and client dependencies from project root
### No need to go into every folder and install them separately for client and server
### For start dev server is command `yarn start` from 'client' folder 
